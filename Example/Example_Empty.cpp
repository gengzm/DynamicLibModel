#include <stdio.h>
#include <format>
#include <random>
#include <Add.h>

int main(){

	//A addd;
	int a = 2; int b = 3;

	//c = addd.add(a, b);
	int c = aaaddd(a, b);
	
	printf("%d + %d = %d\n", a, b, c);


	ADD addd;

	c = addd.add(a, b);

	printf("%d + %d = %d\n", a, b, c);

	return 0;
}